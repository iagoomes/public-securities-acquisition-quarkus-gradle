openapi: 3.0.3
info:
  title: Public Securities Acquisition API
  description: API for managing public securities acquisition processes.
  version: 1.0.0
  contact:
    name: Iago Gomes
    email: iagoomes@example.com

tags:
  - name: Public Securities
    description: Operations related to public securities acquisition.

paths:
  /api/v1/public-securities:
    post:
      tags:
        - Public Securities
      summary: Create a new public securities acquisition record
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PublicSecuritiesAcquisitionRequest'
      responses:
        '201':
          description: Public securities acquisition record created successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicSecuritiesAcquisitionResponse'
        '400':
          description: Bad request
        '500':
          description: Internal server error

  /api/v1/customers/{customerId}/public-securities:
    get:
      tags:
        - Public Securities
      summary: Retrieve public securities acquisition records for a specific customer
      parameters:
        - name: customerId
          in: path
          required: true
          schema:
            type: string
            format: cpf
          description: CPF of the customer
      responses:
        '200':
          description: List of public securities acquisition records for the customer
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PublicSecuritiesAcquisitionResponse'
        '404':
          description: Customer not found
        '500':
          description: Internal server error

components:
  schemas:
    PublicSecuritiesAcquisitionRequest:
        type: object
        properties:
          isin_code:
            type: string
            description: International Securities Identification Number
          issuer_name:
            type: string
            description: Name of the issuer
          quantity:
            type: integer
            description: Quantity of securities to acquire
          unit_price:
            type: number
            format: BigDecimal
            description: Unit price of the security
          buyer_cpf:
            type: string
            format: cpf
            description: CPF of the buyer
    PublicSecuritiesAcquisitionResponse:
        type: object
        properties:
          id:
            type: string
            format: uuid
            description: Unique identifier of the acquisition record
          isin_code:
            type: string
            description: International Securities Identification Number
          issuer_name:
            type: string
            description: Name of the issuer
          acquisition_date:
            type: string
            format: date
            description: Date of acquisition
          quantity:
            type: integer
            description: Quantity of securities acquired
          unit_price:
            type: number
            format: BigDecimal
            description: Unit price of the security
          total_price:
            type: number
            format: BigDecimal
            description: Total price of the acquisition
          buyer_cpf:
            type: string
            format: cpf
            description: CPF of the buyer
          created_at:
            type: string
            format: date-time
            description: Timestamp of when the record was created